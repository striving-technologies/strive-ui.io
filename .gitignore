# General
node_modules/
package-lock.json

# Build outputs
dist
storybook-static
*.tgz
*bundle-analysis.html

# Dev files
debug.log

# Test files
coverage

# Husky internal shims (regenerated by `yarn prepare`)
.husky/_

# Local Claude Code personal settings (shared config is committed)
.claude/settings.local.json

# Disposable per-issue worktrees created by the SDLC agent runner
.agent-worktrees/

# Local runner secrets/config for the systemd/cron units (holds the OAuth token)
scripts/systemd/agent-runner.env